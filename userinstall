pwd=`pwd`

stow alacritty
stow autostart
stow bat
stow compton
stow dunst
stow feh
stow i3
stow ncmpcpp
stow polybar
stow qutebrowser
stow ranger
stow rofi
stow tig
stow tmux
stow vim
stow x
stow zsh

chsh -s $(which zsh)

git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

mkdir -p ~/.icons/cursor-default-theme
cp -r cursor/* ~/.icons/cursor-default-theme

ln -sf $pwd/.fonts.conf ~

cp $pwd/mopidy/.config/mopidy/mopidy.conf $HOME/.config/mopidy/mopidy.conf
