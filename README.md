# Max's dotfiles

Currently supports the following confs:
+ alacritty
+ feh
+ i3
+ ncmpcpp
+ polybar
+ qutebrowser
+ st
+ tig
+ tmux
+ vim
+ zsh

run ``./install.sh`` to install symlinks in your $HOME directory
